package com.plaidcamp.mealogram.domain.history;

import org.springframework.data.jpa.repository.JpaRepository;

public interface HeightHistoryRepository extends JpaRepository<HeightHistory, Long> {
}
